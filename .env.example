# Optional: defaults work locally without an environment file.
PORT=3000
HOST=127.0.0.1
DATABASE_PATH=./data/journal.sqlite
# Public deployment MUST configure HTTPS, Secure cookies, and exact public origin.
# APP_ORIGIN=https://your-domain.example
# COOKIE_SECURE=true
# TRUST_PROXY=1
