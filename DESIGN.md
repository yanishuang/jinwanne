# 今晚呢 · 已选择 B 白蓝风格

当前正式客户端为 SwiftUI 原生 iOS App；网页版本仅作本地预览。两端遵循同一交互与视觉约束。

用户已选择 B（雾白钴蓝），并明确要求进一步简化：首页只有一个「今晚发起了」按钮，点击马上切换到「成功 / 失败」，选择后直接保存。

## 当前范围

- 首页三个状态：发起按钮 → 成功 / 失败 → 已记录，可修改。
- 初始首页不放大标题、统计卡片、日历、历史列表、备注输入或表单。
- 两个底部入口：记录 / 排行榜。设置从右上角进入，历史统计放在设置内。
- 点击「今晚发起了」只切换本机界面，不发送伴侣通知、不创建未完成记录。选择结果后才写入服务器。
- 失败对应原有数据中的 declined（申请被拒绝）；没有选择不自动算失败。
- 同一天只有一个最终结果，修改不重复计天数。匿名榜单继续由服务器汇总，默认不参加。

## 视觉基准

参考图：design/styles/B-white-cobalt.png。风格已选定，图中的复杂首页布局已被用户新的两步流程明确替代。

- 真白背景 #ffffff；桌面外背景 #f5f7fb。
- 钴蓝主色 #1551ee；深蓝黑文字 #18273d；辅助文字 #7b8599。
- 中文无衬线，不使用绿色、宋体、小票、插画或渐变。
- 首页按钮 68px 高；结果按钮 64px 高，左右并列；圆角 9–12px。
- 手机上的正文与控件保持清楚；桌面以最大 480px 的应用宽度呈现。
- 首页内容居中，只有必要日期、核心动作和底部导航。保存成功后用相同蓝色呈现两种结果，不用红色惩罚式反馈。

## App Icon

图标采用「今晚申请票」：一只男人的手在月亮下递出带问号的小票，表达“今晚有戏吗”的调侃与共鸣。保留钴蓝主色，以一枚暖黄色月亮增加趣味；不画妻子、争吵、床或露骨内容，避免把拒绝变成对伴侣的指责。源图与成品位于 `design/icons/`。

## 历史提案

PRD-今晚呢.md、design/tonight-concept.png 和其他风格图保留作历史参考，不能据此重新增加已删功能。当前用户选择优先于这些历史文件。
