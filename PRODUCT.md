# 今晚呢

一个适合手机与电脑浏览器的中文网页 App，面向成年人，用于私人记录向伴侣提出亲密请求后的每日结果。

## 首版边界

- 白蓝风格。首页「今晚发起了」点击后显示「成功 / 失败」，选择即保存；可修改当天结果。发起按钮不创建待办或向伴侣发送消息。
- 历史记录在设置中查看、修改和删除；首页不放日历与统计卡片。失败指被拒绝，未记录不代表被拒绝。
- 同一账号同一自然日只保留一个最终结果，两种结果互斥，不记录次数。
- 统计与排名统一采用 Asia/Shanghai 时区，禁止未来日期与无效日期。
- 服务器持久化保存记录，服务端查询数据库生成成功天数榜和拒绝天数榜，支持本月、今年、全部时间。
- 排名只包含主动参与者且对应天数大于零；天数相同并列排名；退出立即不再出现在新获取的排行榜中。
- 自动生成匿名身份与匿名代号；排行榜不返回私人备注、每日日期或内部账号标识。
- 详细记录只能由对应身份读取。无需伴侣姓名、联系方式或照片，不发送亲密申请或通知给任何人。
- 匿名身份保存在当前浏览器的 HttpOnly Cookie 中；首版不提供跨设备登录，清除 Cookie 后不能找回原身份，明确告知并支持导出。
- 支持永久删除账号与其记录。上线前仍需 HTTPS、持久化磁盘、受控备份与部署方隐私审查。
- 不承诺端到端加密：服务器管理员具有数据库访问能力。

## 产品语气

以尊重双方意愿为前提；拒绝不需要理由。排名仅是用户自行记录的天数，不衡量婚姻质量，不设计羞辱称号、伴侣评价、提醒施压或惩罚。

## 交付形态

原生 SwiftUI iOS App 为正式客户端，支持 iOS 17 及以上。React + Vite 版保留为本地预览；两端共用 Node.js + Express 后端和 SQLite 持久化。无需外部付费服务即可在本机模拟器完整运行；真机与 App Store 发行需要 HTTPS 服务端、Apple Developer 签名和正式 Bundle ID。
